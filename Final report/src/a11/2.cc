int main()
{
    std::string newString;
    removeBlanks(newString); // removes blanks and
		// prints the modified string
    capitalizeString(newString); // capitalizes the original
					// string
              // so it still has blanks if they were present
}
